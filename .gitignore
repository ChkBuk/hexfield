node_modules/
dist/
.astro/
.cache/
# Vercel build output — generated by the Astro Vercel adapter. Never commit:
# the bundle includes platform-specific native binaries (sharp), so a local
# Mac build won't run on Vercel's Linux runtime. Vercel rebuilds on deploy.
.vercel/output/
.env
.env.*
!.env.example
.DS_Store
.netlify/
reports/
*.log
